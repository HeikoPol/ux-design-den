# /etc/caddy/Caddyfile
# Caddy obtains and renews the TLS certificates for both hosts automatically.

uxden.ca {
	encode gzip zstd
	reverse_proxy 127.0.0.1:3000
}

www.uxden.ca {
	redir https://uxden.ca{uri} permanent
}
